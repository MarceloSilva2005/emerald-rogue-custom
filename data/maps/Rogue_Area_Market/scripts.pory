
const FLAG_HIDE_BANK = FLAG_TEMP_1
const FLAG_HIDE_TREATS = FLAG_TEMP_2
const FLAG_HIDE_POKEBLOCK = FLAG_TEMP_3

mapscripts Rogue_Area_Market_MapScripts 
{
    MAP_SCRIPT_ON_LOAD: Rogue_Area_Market_OnMapLoad
}


script Rogue_Area_Market_OnMapLoad
{
    hubupgradeunlocked(HUB_UPGRADE_MARKET_BANK)
    if(var(VAR_RESULT) == YES)
    {
        clearflag(FLAG_HIDE_BANK)
    }
    else
    {
        setflag(FLAG_HIDE_BANK)
    }

    hubupgradeunlocked(HUB_UPGRADE_MARKET_TREAT_SHOP)
    if(var(VAR_RESULT) == TRUE)
    {
        clearflag(FLAG_HIDE_TREATS)
    }
    else
    {
        setflag(FLAG_HIDE_TREATS)
    }

    hubupgradeunlocked(HUB_UPGRADE_MARKET_POKEBLOCK_BLENDERS)
    if(var(VAR_RESULT) == TRUE)
    {
        clearflag(FLAG_HIDE_POKEBLOCK)
    }
    else
    {
        setflag(FLAG_HIDE_POKEBLOCK)
    }
}



script Rogue_Area_Market_ShopBank
{
    lock
    faceplayer
    message(format("Bem-vindo ao Banco!\pPosso fornecer Coin Cases que viram Pokédollars automaticamente quando você inicia uma Aventura.\pQuer comprar algumas Coin Cases?"))
    waitmessage

    pokemart(BankMart)

    msgbox("Volte sempre!")
    release
}

script Rogue_Area_Market_BankSign
{
    hubupgradeunlocked(HUB_UPGRADE_MARKET_BANK)
    if(var(VAR_RESULT) == YES)
    {
        msgbox(format("Banco"), MSGBOX_SIGN)
    }
}

mart BankMart
{
    ITEM_SMALL_COIN_CASE
    ITEM_LARGE_COIN_CASE
}


script Rogue_Area_Market_BlendPokeblockNovice
{
    setflag(FLAG_HIDE_LILYCOVE_CONTEST_HALL_BLEND_MASTER)
    
    lock

    // NOVICE_BERRY_COUNT
    msgbox(format("Olá!\nSou iniciante em mistura de {POKEBLOCK}.\pPosso produzir até 10 {POKEBLOCK} usando 20 Berries.\pQuer fazer uma mistura comigo?"), MSGBOX_YESNO)

    if(var(VAR_RESULT) == YES)
    {
        specialvar(VAR_RESULT, PlayerHasBerries)

        if(var(VAR_RESULT) == YES)
        {
            // NOVICE_BERRY_COUNT
            setvar(VAR_0x800A, 20) // num berries

            setvar(VAR_RESULT, YES)
            while(var(VAR_RESULT) == YES)
            {
                call(Rogue_Area_Market_BlendPokeblockNoviceLoop)
            }
        }
        else
        {
            msgbox(format("Ah.\pParece que você está sem Berries para misturar."))
        }
    }
    else
    {
        msgbox(format("Entendo.\nSe mudar de ideia, fale comigo de novo."))
    }

    release
}

script Rogue_Area_Market_BlendPokeblockNoviceLoop
{
    msgbox(format("Qual Berry você quer misturar?"))

    fadescreen(FADE_TO_BLACK)
    special(Bag_ChooseBerry)
    waitstate

    if(var(VAR_ITEM_ID) != 0)
    {
        special(Rogue_IsValidPokeblockBerry)
        if(var(VAR_RESULT) == YES)
        {
            // NOVICE_BERRY_COUNT
            checkitem(VAR_ITEM_ID, 20)

            if(var(VAR_RESULT) == YES)
            {
                msgbox(format("Certo.\pVamos misturar!"))

                waitse
                setvar(VAR_0x8004, 1) // NUM_OPPONENTS
                fadescreen(FADE_TO_BLACK)
                special(DoBerryBlending)
                waitstate

                call(Rogue_Common_DisplayPopups)

                msgbox(format("Quer misturar mais Berries?"), MSGBOX_YESNO)
            }
            else
            {
                // NOVICE_BERRY_COUNT
                msgbox(format("Você precisa de 20 Berries para fazer essa mistura."))
            }
        }
        else
        {
            msgbox(format("Essa Berry fica sem efeito na mistura de {POKEBLOCK}.\pUse Check Tag na Bag para ver qual {POKEBLOCK} cada Berry produz."))
        }
    }
    else
    {
        setvar(VAR_RESULT, NO)
    }
}

script Rogue_Area_Market_BlendPokeblockMaster
{
    clearflag(FLAG_HIDE_LILYCOVE_CONTEST_HALL_BLEND_MASTER)
    
    lock

    // MASTER_BERRY_COUNT
    msgbox(format("Olá!\nSou mestre em mistura de {POKEBLOCK}.\pPosso produzir até 30 {POKEBLOCK} usando 70 Berries.\pQuer fazer uma mistura comigo?"), MSGBOX_YESNO)

    if(var(VAR_RESULT) == YES)
    {
        specialvar(VAR_RESULT, PlayerHasBerries)

        if(var(VAR_RESULT) == YES)
        {
            // MASTER_BERRY_COUNT
            setvar(VAR_0x800A, 70) // num berries

            setvar(VAR_RESULT, YES)
            while(var(VAR_RESULT) == YES)
            {
                call(Rogue_Area_Market_BlendPokeblockMasterLoop)
            }
        }
        else
        {
            msgbox(format("Ah.\pParece que você está sem Berries para misturar."))
        }
    }
    else
    {
        msgbox(format("Entendo.\nSe mudar de ideia, fale comigo de novo."))
    }

    release
}

script Rogue_Area_Market_BlendPokeblockMasterLoop
{
    msgbox(format("Qual Berry você quer misturar?"))

    fadescreen(FADE_TO_BLACK)
    special(Bag_ChooseBerry)
    waitstate

    if(var(VAR_ITEM_ID) != 0)
    {
        special(Rogue_IsValidPokeblockBerry)
        if(var(VAR_RESULT) == YES)
        {
            // MASTER_BERRY_COUNT
            checkitem(VAR_ITEM_ID, 70)

            if(var(VAR_RESULT) == YES)
            {
                msgbox(format("Certo.\pVamos misturar!"))

                waitse
                setvar(VAR_0x8004, 1) // NUM_OPPONENTS
                fadescreen(FADE_TO_BLACK)
                special(DoBerryBlending)
                waitstate

                call(Rogue_Common_DisplayPopups)

                msgbox(format("Quer misturar mais Berries?"), MSGBOX_YESNO)
            }
            else
            {
                msgbox(format("Você precisa de 70 Berries para fazer essa mistura."))
            }
        }
        else
        {
            msgbox(format("Essa Berry fica sem efeito na mistura de {POKEBLOCK}.\pUse Check Tag na Bag para ver qual {POKEBLOCK} cada Berry produz."))
        }
    }
    else
    {
        setvar(VAR_RESULT, NO)
    }
}

script Rogue_Area_Market_TalkBerryBlender
{
    lock
    msgbox(format("Eles parecem totalmente focados na Berry Blending Machine."))
    release
}

script Rogue_Area_Market_TalkPokeblockHelper
{
    lock
    faceplayer
    msgbox(format("Você parece ser Trainer!\pEu entendo bastante de {POKEBLOCK}.\pPosso explicar quais Berries produzem cada tipo de {POKEBLOCK}, se quiser."))

    setvar(VAR_RESULT, YES)

    while(var(VAR_RESULT) == YES)
    {
        begindynamicmultichoice(32)
        special(Rogue_AppendMultichoicePokeblockItems)
        message(format("Que tipo de {POKEBLOCK} você procura?"))
        waitmessage
        enddynamicmultichoice(19, 2, 5, TRUE)

        if(var(VAR_RESULT) != MULTI_B_PRESSED)
        {
            copyvar(VAR_0x8004, VAR_RESULT)
            addvar(VAR_0x8004, FIRST_ITEM_POKEBLOCK)
            bufferitemname(STR_VAR_1, VAR_0x8004)

            begindynamicmultichoice(32)
            special(Rogue_AppendMultichoiceBerriesForPokeblock)
            message(format("Estas Berries produzem {STR_VAR_1}."))
            waitmessage
            enddynamicmultichoice(19, 2, 5, TRUE)
            
            setvar(VAR_RESULT, YES)
        }
        else
        {
            setvar(VAR_RESULT, NO)
        }
    }

    release
}